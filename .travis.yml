language:
  - python

python:
  - 2.7

install:
  # g++4.8.1
  - if [ "$CXX" = "g++" ]; then sudo apt-get install -qq g++-4.8; fi
  - if [ "$CXX" = "g++" ]; then export CXX="g++-4.8"; fi
  - ./install.sh

script:
  - $CXX --version
  - ./run_tests.sh
  - ./run_tests_static.sh

